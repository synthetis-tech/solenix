# solenix.yaml — конфигурация solenix-core сервера
# Запуск: solenix serve --config solenix.yaml

grpc_addr: 8731

http_addr: 8080

database: "default"

wal_max_size: 32 # MiB

flush_interval: "2m"

compaction_threshold: 10

retention: "720h"

collector:
  enabled: true
  interval: "1s"
